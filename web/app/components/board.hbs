<div class="board-container">
  <div class="board">
    {{#each @board as |row|}}
      {{#each row as |cell|}}
        <Cell @cell={{cell}} />
      {{/each}}
    {{/each}}
  </div>
  {{yield}}
</div>